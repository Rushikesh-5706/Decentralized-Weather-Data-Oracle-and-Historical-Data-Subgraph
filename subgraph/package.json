{
    "name": "weather-oracle-subgraph",
    "license": "MIT",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "auth": "graph auth --studio $GRAPH_ACCESS_TOKEN",
        "deploy": "graph deploy --studio weather-oracle-sepolia --version-label v1"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "^0.68.0",
        "@graphprotocol/graph-ts": "^0.32.0"
    }
}